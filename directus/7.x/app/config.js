/* eslint-disable */

(function directusConfig() {
  const config = {
    api: {
      'http://localhost:8080': 'Directus Demo API'
    },
    allowOtherAPI: false
  };

  window.__DirectusConfig__ = config;
}());
